/**
 * Centralized User Configuration
 * Ganti nilai di sini untuk demo kompetisi — seluruh app akan mengikuti.
 */
export const CURRENT_USER = {
  /** Document ID di Firestore (Users/{id}, Sesi_Aktif/{id}, Riwayat where user=={id}) */
  id: "Nayaka",
  /** Nama lengkap yang ditampilkan di UI */
  displayName: "Nayaka Alkaesyah S.",
} as const;
